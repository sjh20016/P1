# Portal Character 0.01 — 开发交付

> 这是原双门版本的交付记录。当前默认采用快速魔法操作，见 [空间破坏操作迭代](PORTAL-MAGIC.md)。要复现本文手动 A/B、Q 操作，请进入原型后先按 F4。

本版本是可运行的独立角色原型。已验证「自由落体 → 地面入口 → 墙面出口 → 动量弹射 → 撞开建筑」和「两点切割 → 原有建筑结构断裂」。尚未进行真人趣味性验收，不将自动测试通过等同于“已经足够好玩”。本轮到此为止，不开展 0.02。

## 立即试玩

双击本目录 **开始传送门原型.cmd**。首次导入可能需要等待。启动脚本优先使用 `GODOT_EXE`，否则寻找项目工具目录与本机已安装的 Godot 4.7.1。

1. 按 **F6**：以零初速从 48 m 高处落下，观察 A → B → 建筑前后墙。只改变试验起点，未注入弹射速度。
2. 按 **F5** 全重置，再按 F6 重复；按 **R** 只回当前测试区起点，不修复已毁建筑。
3. 按 **2** 测试水平穿门：WASD、空格跳跃、左键放 A、右键放 B。墙上的门中心需要足够高，完整圆面必须有支撑。
4. 按 **L** 进入 E 区并投放循环刚体；等待加速后，**左键把 A 出口移到另一面墙**，将循环变为弹丸线路。B 是地面入口。
5. 按 **6**、**Q**：观察两点切割与弱缝塔倒塌。F5 可恢复塔；切割不是 Mesh Boolean。
6. 按 **F3** 展开实时参数；Esc 释放/捕获鼠标。F3 编辑期间玩家运动输入暂停，世界物理仍运行。

| 操作 | 功能 |
| --- | --- |
| WASD / 空格 | 共用移动 / 跳跃 |
| 左键 / 右键 | A / B，保留旧门直到新位置放置成功 |
| Q | Spatial Cut，默认冷却 2 秒 |
| E | 坠落时生成持续 2 秒的临时 A；需要预先放好 B |
| T | 向视线方向投放小刚体；最多 8 个 |
| X | 清除两门 |
| 1–8 | 选择测试区，快速放置预设门并回到该区起点 |
| R / F5 | 玩家复位 / 整场重新加载 |
| F6 / L | 零初速高差试跳 / 碎块重力循环实验 |

## A. 已完成功能

- 静态墙面、地面、天花板及完整建筑外壳上的 A/B 放置。中心与八个圆周采样验证平面支撑，拒绝斜面、移动体、小表面和距离过近的门。瞄准提供 A 放置预览；B 点击时独立验证。
- 双向位置、方向、速度坐标转换。默认保持速率，可在 0.8–1.2 调整动量倍率。球体前缘扫掠检测在碰撞实体墙之前接管穿越，避免依赖离散 Area 重叠。
- 出口球形占用和运动路径检查；有限偏移搜索无解时将旅客弹回入口。每个旅客有独立时间冷却。
- Portal 与基础移动、摄像机和原有伤害系统组合，未把能力塞进 PlayerController。独立角色加载时移除触手能力节点。
- 通用动能冲击适配：记录速度、质量和真实能量；用平方速度曲线转换成现有游戏强度。强度上限 100，伤害半径 2.6 m，每帧玩家最多处理四次接触。
- 小型球/盒刚体保留朝向、线速度与角速度。使用物理积分回调修改刚体状态，开启 CCD。大型或复杂碰撞体不接入。
- Spatial Cut：70 ms 预告黑线，连接两门的有限宽度区域查询既有逻辑墙盒，投递 SLASH 伤害；局部预切割、弱缝断裂和宏块倒塌复用原实现。
- 手动坠落救援；自动救援只在调试面板勾选后启用，默认关闭。
- 黑洞单环 / 双环区分 A/B；复用黑色伤痕、碎块、碰撞声音、CameraShake 与高能量 Hit Stop；新增速度风声、出门 FOV 与短时镜头恢复。
- 独立 `Portal_Playground` 八区：A 高差、B 水平、C 90°、D 撞墙、E 碎块、F 切割、G 两门连续组合、H 可破坏塔。

## B. 主要文件

| 文件 | 职责 |
| --- | --- |
| `project/scenes/portal/Portal_Playground.tscn` | 独立启动场景 |
| `project/scripts/portal/portal_playground.gd` | 小测试场、角色组合、预设线路、刚体投放与重置 |
| `portal_profile.gd` / `portal_debug_hud.gd` | 参数资源与实时调试面板 |
| `portal_component.gd` | 门的表面身份、有效性、占位表现 |
| `portal_physics.gd` | 纯坐标旋转、速度限幅与扫掠几何 |
| `portal_manager.gd` | 放置、旅客冷却、出口检查、刚体物理扩展 |
| `portal_ability.gd` | 输入、玩家运动事件接入、临时入口、预览 |
| `spatial_cut_ability.gd` | 切割预告、查询和现有伤害事件适配 |
| `portal_feedback.gd` | 穿越镜头、FOV、风声与资源清理 |
| `portal_destruction_zone.gd` | 复用 OpenZone 宏块逻辑的四塔常驻适配 |
| `project/scripts/shared/kinetic_impact.gd` | 可供其他角色调用的动能事件与伤害适配 |
| `project/scripts/destruction/debris_piece.gd` | 唯一修改的既有运行模块：增加可选、弱引用的通用物理扩展 |
| `project/scripts/debug/test_portal001.gd` | 物理与异常处理自动测试 |
| `project/scripts/debug/playtest_portal001.gd` | 正常渲染/音频、真实输入游玩巡检与截图 |
| `tools/portal.ps1` | 启动、导入检查、测试与渲染巡检入口 |

表中省略目录的脚本均位于 `project/scripts/portal/`。

## C. 架构与来源

```text
RavagePlayer (原有移动) ─motion_requested─> PortalAbility
                                             ├─ PortalManager / PortalPhysics
                                             └─ KineticImpact
RigidBody ─_integrate_forces─> 可选物理扩展 ──────┘

KineticImpact / SpatialCutAbility
  → RavageDamageEvent → DestructionManager
  → OpenDamageState / OpenTower → 原有碎块和 OpenMacro
  → 原有反馈、Hit Stop、CameraShake
```

基础移动与摄像机输入直接复用；Impact 增加独立适配器；Damage / Destruction 复用已有公共事件。没有为了 Portal 重写整个移动系统，也没有声称把既有所有触手逻辑彻底拆分完成。原来的触手主场景入口仍保持原样。

原工作区 `P1` 是未完成的克隆，没有可用 HEAD。开发使用相邻的已核验 0.07 源码快照 `D:\D-game\P1-codex-consequence-004\P1-codex-consequence-004`，复制到本目录后建立独立 Git 仓库，分支 **portal/prototype-0.01**，导入基线提交 **06b5a96**。原快照对应此前记录的上游 `b0d24b60b88f755d4ff1890b13dd1af0c1fa1677`；新仓库没有伪造上游提交历史，也没有推送远端。

实现参考了 Godot 官方的 [物理空间查询](https://docs.godotengine.org/en/stable/classes/class_physicsdirectspacestate3d.html) 和 [RigidBody3D 积分回调](https://docs.godotengine.org/en/stable/classes/class_rigidbody3d.html)。

## 验证证据

在 Windows、Godot **4.7.1.stable.official.a13da4feb** 上实际执行。源码原本标注 4.7.2，本轮没有用 4.7.2 重新验收。

- [物理测试结果](docs/portal001/test-results.json)：48 项检查，零失败。零初速坠落后水平速度 **46.777 m/s**，先后破坏建筑两面墙；60 Hz、140 m/s 试验穿越一次且出口速度约 139.25 m/s；刚体循环 27 次，最大速度 100 m/s。
- [渲染输入巡检](docs/portal001/rendered-playtest.json)：正常 OpenGL Compatibility、NVIDIA RTX 5060 Laptop GPU、WASAPI 音频；实际发送鼠标点击、W/跳跃、Q、F3、Esc、F5，并验证重置后的重复弹射。八区均被访问，C/D/G/H 的自由路线玩法尚未逐一由真人评价。
- [原始渲染帧时间](docs/portal001/rendered-frames-ms.json) 与七张截图同时保存；这是约半分钟的自动巡检，不是五分钟压力验收。截图保存和重载区间也在样本内。
- 最终 4,491 帧，共 25.45 秒：P95 **6.356 ms**、P99 **8.006 ms**、最大 **175.943 ms**，9 帧超过 33.33 ms。存在长帧，未完成热点归因；不能用 P95 掩盖最大值，也不能与旧 36 塔数据直接比较。
- 既有 `movement`、`grapple`、`destruction`、`lifecycle`、`open006`、`dual_pull007` 六组回归通过。证明现有覆盖的触手路径仍工作，不代表所有旧版功能都已穷尽测试。
- 本轮发现并修正：循环试验的默认角阻尼、出口镜头短暂进入墙体、音频资源释放、用错循环出口提示，以及测试之间提前毁坏目标的问题。
- 一次渲染复测在 E 区之后未响应自动按键。巡检脚本关闭输入合并并加入实际区域/控制状态记录后，最终 A–H 切换和 Q、重置均通过。原失败的具体原因未独立复现；跑自动巡检时需避免手动操作同一个游戏窗口。

复现命令：

```powershell
.\tools\portal.ps1 -Check
.\tools\portal.ps1 -Tour
# 可显式选择引擎：
.\tools\portal.ps1 -Engine '你的 Godot 控制台程序绝对路径'
```

## D. 已知限制

- **卡位与碰撞**：0.01 在球体前缘接触门面时转换，整帧运动在出口继续；不是实体逐面裁切，可能产生小于一帧位移的额外行程。出口检查偏保守，有障碍时可能拒绝一个肉眼看似能挤出的出口，优先避免穿墙和卡死。
- **建筑表面**：合并碰撞壳被重建时直接关闭依附旧壳的门，即使邻近墙片仍完整。需要重新放置；尚无稳定表面 ID 的重附着。
- **刚体**：保守包围球有尺寸限制，仅球/盒形状；大型结构、凸分解、连接体和宏块不穿门。正常阻尼/碰撞在穿越之外仍可改变角速度。普通新碎块不会自动获得额外撞击伤害，穿门后的碎块有 5 秒动能伤害资格。
- **性能**：既有真实碎块上限 48、宏块 3，额外投放体上限 8；没有新增物理约束。检测每个旅客最多检查两门。未进行完整 36 塔地图的 Portal 性能对比；渲染巡检仍可能出现首用/截图长帧，不能据此宣称所有尖峰已解决。
- **摄像机**：保持世界竖直，不保留身体滚转；出门立即变换朝向、短时收起再恢复第三人称距离。可读性已通过截图检查，眩晕和冲击感仍需真人评价。未启用 Motion Blur。
- **切割**：沿既有格子/弱缝破坏，每栋建筑每次最多一个事件；裂口宽度和倒塌形状受现有逻辑墙格限制，并非任意精确切线。
- **地图与角色**：四塔灰盒、基础黑球角色；参考图只作为黑白方向参考，没有制作角色模型。高处起点的自主落线需要练习，可用 F6 快速观察成功路线。
- **试玩范围**：完整自动输入巡检已执行；没有人类受试者，因此“爽”“愿意重复”“自发产生新玩法”未被证实。

## E. 推荐参数

| 参数 | 默认建议 | 说明 |
| --- | ---: | --- |
| PortalPlacementRange | 150 m | 距离相机射线起点 |
| PortalSize | 3.2 m | **半径**，直径 6.4 m；改动会清除旧门 |
| ExitOffset | 0.22 m | 在旅客半径和安全边界之外 |
| MomentumMultiplier | 1.0 | 先验证守恒，再比较 0.9 / 1.1 |
| MaxPortalVelocity | 100 m/s | 测试上限可调到 140 |
| PortalCooldown | 0.18 s | 每个旅客单独计时 |
| PortalObjectMassLimit | 25 | 引擎质量单位 |
| SpatialCutDamage / Width / Cooldown | 55 / 1.2 m / 2 s | 沿用原伤害强度单位 |
| HighSpeedImpactThreshold | 26 m/s | 法向速度，不是擦边总速率 |
| EmergencyPortalEnabled | false | 自动救援实验开关 |
| PortalCameraBlend | 0.06 s | 出口第三人称距离恢复 |
| PortalFOVBoost / HighSpeedFOV | 10° / 92° | 可设 Boost=0 比较舒适度 |
| ExitCameraStabilization | 0.12 s | 出门短暂俯仰稳定 |

## F. 七个测试问题与下一阶段

| 问题 | 当前证据 / 判断 |
| --- | --- |
| 会主动利用高度吗？ | 高差确实提供约 46.8 m/s 弹射；主动选择高度的行为需要真人观察。 |
| 是移动工具还是破坏工具？ | 已能把坠落与循环刚体转为建筑伤害，具备破坏工具的机械条件。 |
| 是否比普通撞击更爽？ | 普通低速移动不触发相同强度的破坏；高能碰撞有顿挫和碎裂。主观差异未验证。 |
| 会尝试奇怪角度和连续门吗？ | 平面 90° 转向、两门重放置和循环可运行；当前不支持任意坡面，不能声称已经观察到自发尝试。 |
| Spatial Cut 有独立价值吗？ | 无需身体撞击就能断弱缝并倒塌，功能区别成立；布置成本与乐趣待测。 |
| 和开放塔林有互动吗？ | 复用真实塔壳、局部裂口、宏块与废墟已验证；整片开放塔林未接入 Portal。 |
| 愿意花时间布置吗？ | 未验证。已提供预览、失败原因、预设测试线路与快速重置，先观察布置耗时/成功率。 |

建议下一步先进行 10–15 分钟真人试玩，记录首次自由布置成功时间、主动重试次数、第一次主动改变出口的位置，以及是否出现“还能这样玩”的尝试。若这些行为出现，再考虑强化 Portal Momentum；若主要时间花在对准和失败放置上，先改快速施法、吸附或落点预览，不追加新技能。当前证据支持继续测试动量核心，尚不足以正式批准 0.02。
