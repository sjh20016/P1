# Portal 0.01 · 空间破坏操作迭代

> 最新混合操作见 [技能与物理门共存](PORTAL-HYBRID.md)：魔法模式内可用 Q + 左/右键布置实体门，F 冲门、G 改出口，并透过门观察另一侧。本文保留上一轮技能参数与验证记录。

默认玩法已改为快速空间突进、头脚回路蓄速和蓄力空间切割。双击 **开始传送门原型.cmd** 进入独立测试场；原触手角色仍使用 **开始游戏.cmd**。本轮沿用 `portal/prototype-0.01` 分支，不进入 0.02。

## 先玩这三步

1. 瞄准前方白塔，**点左键**：自动生成脚下入口，在目标前方出门，以至少 42 m/s 撞过去。按住左键可以用鼠标引导高速飞行的出口预览，看到合适位置就松开；空中同样可用。
2. **按 Shift** 开始头脚回路蓄速，约 1.1 秒达到 100 m/s。**再按 Shift** 沿视线发射；也可以在蓄速中按住左键选择出口，再松开发射。用 **F5** 重建已毁建筑，重复比较普通突进和蓄速撞击。
3. **长按空格** 短暂悬浮，用鼠标引导切割标记；击中建筑或松开空格即锁定。随后**按住右键**扩大切割直径，松开后两个重叠黑门上下分离，向原有破坏系统施加水平断层切割。H 区（按 8）有弱缝塔可测试倒塌。

| 操作 | 行为 |
| --- | --- |
| WASD / 短按空格 | 原有基础移动、空中微调 / 跳跃 |
| 左键按住 → 松开 | 引导出口 → 自动脚下入口、转向突进；短点为快速施法 |
| Shift → Shift | 开始蓄速 → 沿视线发射 |
| 蓄速时左键按住 → 松开 | 保留蓄速，精调出口后发射 |
| 空格长按 → 松开 | 短暂悬浮并发射切割标记 → 锁定标记、缓慢下落 |
| 有标记时右键按住 → 松开 | 扩大直径 → 双门分离切割 |
| X | 取消当前选点或蓄速；已释放的切割继续完成 |
| R / F5 | 回本区起点、取消能力 / 重建整个测试场并保留操作模式 |
| F3 / Esc | 实时参数面板 / 释放或捕获鼠标 |
| F4 | 魔法操作与旧双门操作切换 |
| 1–8 / F6 / L / T | 八区预设 / 高差坠落试验 / 碎块循环 / 投放碎块 |

旧版手动 A/B、Q 两点切割、刚体动量传送和高差试验保留在 F4 模式中，详细说明见 [原 0.01 交付](PORTAL-0.01.md)。F5 重建后仍保留所选模式，启动新进程则默认魔法操作。

## 实现与设计取舍

- **自动入口、自由出口。** 不要求先布置两面墙。轻点左键沿视线快速取点；长按时预览以 115 m/s 飞行，并跟随鼠标转向。使用连续射线拦截建筑，未创建隐形触手、绳索或约束。撞上表面后，预览环显示在墙前的预计出口位置；实际释放时再检查占用与路径，必要时向后寻找安全位置。位置无解则拒绝释放，保留蓄速。
- **以真实撞击完成破坏。** 出口与目标间默认保留 7 m 距离，角色以所选方向和保留的速率继续运动，经现有动能事件适配触发伤害。普通突进也有最低破坏速度，帮助快速移动直接进入破坏循环。空中可以再次选出口、重定向当前高速运动。
- **头脚回路是稳定的蓄速状态。** 两个门围绕角色显现，存储速率持续增长并限幅，摄像机保持稳定。没有逐帧真的把角色从脚传到头；这是为避免镜头抖动而采用的原型取舍。发射时才将存储速率写入角色运动；取消时恢复进入回路前的速度。回路暂时不限制最长保持时间。
- **切割是水平圆盘体积。** 空格长按阈值为 0.18 秒，最大悬浮 1.2 秒；标记碰撞、飞行到距离上限、松键或悬浮结束时锁定。锁定后最多 3 秒缓落，避免无限悬浮。右键充能 1.4 秒，直径从 4.8 m 增长至 28 m。释放后约 80 ms 施加伤害，双门上下分离并在 0.8 秒内消失。
- **复用原破坏层。** 体积切割查询现有墙盒，以 `SLASH` 事件交给 `DestructionManager`。`OpenDamageState` 仅增加可选 `slice_span`，使充能影响实际切开的横向范围；未携带此字段的触手扫切沿用原逻辑。没有 Mesh Boolean，也没有第二套建筑伤害系统。

## 主要文件与架构

| 文件 | 作用 |
| --- | --- |
| `project/scripts/portal/portal_magic.gd` | 魔法能力状态机、按键命令、蓄速、悬浮和慢落；不写入 PlayerController |
| `project/scripts/portal/portal_guide.gd` | 鼠标引导的扫掠标记与半透明出口预览 |
| `project/scripts/portal/portal_volume_cut.gd` | 双门分离表现、有上限的圆盘查询和伤害适配 |
| `project/scripts/portal/portal_manager.gd` | 自动脚下入口、安全出口、动量释放；继续承担旧旅客传送 |
| `project/scripts/portal/portal_ability.gd` | 组合新能力与原移动事件、动能撞击接口 |
| `project/scripts/portal/portal_profile.gd`、`portal_debug_hud.gd` | 参数、限幅与可滚动调试面板 |
| `project/scripts/portal/portal_playground.gd` | 默认魔法庭院、旧模式切换、重建恢复 |
| `project/scripts/open/open_damage_state.gd` | 可选切割跨度；其余伤害路径不变 |
| `project/scripts/debug/test_portal_magic.gd` | 状态/物理测试、渲染输入巡检及截图记录 |
| `tools/portal.ps1` | 启动、双模式回归、新旧带画面巡检 |

共享移动、摄像机、`KineticImpact`、`RavageDamageEvent` 与现有破坏管理器继续复用。本轮增加能力模块，没有再扩大基础玩家脚本。空间切割每次最多派发 16 个目标面事件，不创建刚体；既有碎块 48、宏块 3 的预算继续生效。

## 推荐首轮参数

| 参数 | 当前值 | 调整目的 |
| --- | --- | --- |
| `dash_min_speed` / `max_portal_velocity` | 42 / 100 m/s | 普通突进已能破坏；蓄速保留明显差异 |
| `boost_start_speed` / `boost_acceleration` | 32 m/s / 62 m/s² | 静止起蓄约 1.1 秒到顶；已有高速则更快 |
| `guide_speed` / `guide_range` | 115 m/s / 110 m | 快速选点，保留短暂可调整时间 |
| `quick_cast_distance` / `impact_runup` | 42 m / 7 m | 轻点施法距离 / 出门后的撞击距离 |
| `dash_cooldown` | 0.28 秒 | 可连续重定向，避免同帧连发 |
| `cut_min_radius` / `cut_max_radius` | 2.4 / 14 m | 切割直径为 4.8–28 m |
| `cut_charge_time` / `spatial_cut_cooldown` | 1.4 / 2 秒 | 大范围切割节奏 |
| `spatial_cut_damage` / `spatial_cut_width` | 55 / 1.2 m | 事件强度 / 水平断层厚度 |
| `high_speed_impact_threshold` | 26 m/s | 共享冲击触发阈值 |
| `momentum_multiplier` / `portal_size` | 1.0 / 半径 3.2 m | 旧双门动量 / 孔径；新蓄速由上述独立参数控制 |
| `emergency_portal_enabled` | false | 旧自动救援仍为实验开关 |

F3 调整可立即影响后续施法，改动不持久保存；默认值在参数模块中。`PortalSize` 为旧物理门孔径，切割圈及头脚装饰门的尺寸独立。

## 验证和限制

使用本机 Godot **4.7.1 stable**。运行 `powershell -ExecutionPolicy Bypass -File tools/portal.ps1 -Check` 会依次执行旧门 48 项和新魔法 43 项检查；`-Tour` 启动有画面的新操作测试，`-Tour -Classic` 保留旧巡检。脚本要求退出成功、存在 RESULT 且无报错，避免超时退出被误报为通过。

结果和截图保存在 [docs/portal-magic](docs/portal-magic)。无界面测试覆盖命令、状态与真实物理；带画面测试通过 Godot 输入事件入口发送鼠标和键盘事件，开启音频与 Hit Stop。两者均为自动巡检，不能替代真人手感反馈。共享层另跑开放塔、双塔牵引、扫切和基础破坏回归。

本轮结果：新版命令/物理 **43/43**、带画面输入 **43/43**、旧门 **48/48**；共享层开放塔 **17/17**、双塔牵引 **16/16**、扫切 **8/8**、基础破坏 **12/12**。零测试失败、脚本错误或泄漏报告。普通突进实测速度约 41.2 m/s（运动一帧后），蓄速达到并保持 100 m/s；同一组目标上小切割命中 1 处，满蓄力命中 2 处。最后一轮渲染采样 2,057 帧，P95 为 **6.336 ms**，最大 **61.307 ms**；仍存在峰值卡顿，不能据此宣称性能问题已解决。

- 角色仍为占位黑色形体，没有制作参考图的完整角色模型、动画或高质量传送门渲染。
- 极近距离、狭窄夹缝、活动碎块堵塞可能拒绝释放；预览显示预计位置，最终安全检查可能后移出口。没有为高速选择实现全程碰撞体积预览。
- 连续高速出门与原移动阻尼、碰撞顿挫的组合需要真人体验；蓄速镜头稳定，但大角度方向变化仍可能突兀。
- 切割按当前直立塔壳的逻辑墙盒和网格单元近似处理；任意倾斜建筑、复杂连接体、精细曲面不在验收范围内。选中空中可放空技能；单独缺口和特殊弱缝倒塌的收益不同。
- 自动化过程中保持了碎块与宏块预算；尚未证明大地图长期连锁破坏的帧时间稳定。帧时间原始样本包含启动及破坏峰值，见渲染记录，不用平均 FPS 掩盖卡顿。
- 回路可以原地持续保持，缓落有时间上限。若试玩表明蓄速等待影响节奏，应优先降低蓄速时间或让回路允许轻微移动，而不是增加技能。

下一步先真人试三轮：轻点突进连续撞塔、空中改向后蓄速再撞、短蓄力与满蓄力切割对比。记录是否会主动尝试新落点、是否愿意反复蓄速，以及选点时间是否值得。若这些行为自然出现，再强化 Portal Momentum；若玩家主要在等待或找准目标，则先缩短选点/蓄速流程。当前不继续开发 0.02。
