# RAVAGE 0.04 CONSEQUENCE — 最终验证

2026-09-14，Windows x64，Godot 4.7.2 Stable `ed1daf0bf`，Compatibility，Intel UHD Graphics 730，1280×800 窗口 / 1440×900 设计视口，物理 120 Hz。

## 回归结果

`tools/run_checks.ps1 -Rnd -Consequence -Stress`：19 个脚本全部通过，最终 `ALL CHECKS PASSED`，进程返回 0，无脚本错误、引擎错误或泄漏警告。

继承的移动、抓钩、破坏、扫切、VFX、生命周期、完整塔林、墨迹、目标辅助、M03、碰撞恢复、八站路线均通过。新测试覆盖局部洞口、伤痕跟随、三个 Collider 比例、真实输入撞穿、真实 Sweep、支撑断连、延迟倾倒、二次损伤、静态废墟、4/6/8 宏块预算、384 伤痕及连锁深度。

真实 RAM 路线穿过前后两面墙，离塔约 37.493 m/s，并能用正常目标辅助抓住出口梁。固定切断夹具产生一次真实 COLLAPSE、深度 1、一个静态废墟，最终活跃宏块和碎片均为 0。深度 2 接受 / 深度 3 拒绝由独立断言验证。

三分钟、21,600 个物理帧长期回归：峰值速度 55.903 m/s、38 个碎片刚体、241 个旧墨迹、243 次破坏，无 NaN 或超速帧，结束后碎片全部过期。

开发期原生退出异常通过移除玩家 / 抓钩的脚本类型循环处理；没有改运动算法。转储与源码对照、验证边界见 [SHUTDOWN_AUDIT](rnd004/SHUTDOWN_AUDIT.md)。

## 实际图形运行

`capture_consequence.gd` 正常退出，图形错误日志为空。无 HUD / 场景文字的同角度截图显示 RAM 的局部缺口、放射纹与 SLASH 的窄断口、错位残唇。另拍摄抖动延迟、倾倒、静态残骸和接触面近景。最后一张能看见后塔顶缘的新缺损与黑色痕迹。

`capture_consequence_performance.gd` 每场景预热 2 秒、测量约 12 秒；测量期间不截图，不与其他游戏测试并行。FPS 由实际帧间隔总和计算，p95 使用 nearest-rank；物理最大值来自每 0.5 秒采样，不声称是每个物理步的精确最大值。

| 场景 | 平均 FPS | p50 / p95 帧耗时 ms | 最大帧 ms | 物理采样最大 ms | 宏块峰值 | 最终二次事件 / 废墟 |
|---|---:|---:|---:|---:|---:|---:|
| 单塔倒向后塔 | 59.75 | 16.685 / 19.291 | 46.151 | 11.018 | 1 | 1 / 1 |
| 三塔 1,152 墙板 + 384 伤痕 | 59.74 | 16.698 / 17.556 | 69.004 | 1.416 | 0 | 0 / 0 |
| 两塔同时脱离 | 59.68 | 16.697 / 19.374 | 46.133 | 12.380 | 2 | 7 / 2 |

本次单座墙板展开 4.873–11.710 ms，大块构建 1.476–3.738 ms。两塔突发场景碎片每 0.5 秒采样峰值 32；严格 48 上限另由自动测试验证。所有场景结尾活跃大块与碎片均为 0。

首次激活仍会卡顿。字重调整前的一轮曾测到 122.262 ms 最大帧，原数据也保留；不能把后一次更快归因于字重修改，驱动缓存及运行时状态可能影响结果。本机短实验稳定帧率不代表全城高级破坏的性能。旧塔林大量分段时约 30–38 FPS 的历史限制仍保留。

## 交付与证据

`rnd004/evidence/` 保存完整回归摘要、各新测试日志、实际图形日志、JSON 原始样本及选定截图。`PLAYTEST_004.md` 区分自动启动、脚本断言、AI 可观察形态和真人体验判断。是否爽、是否主动利用废墟改变路线，没有由自动化做结论。

高级后果仅覆盖独立实验区域三座塔，原 358 栋保留旧架构。D06 双钩 Pull 未开放。世界废墟在本局保留，重开程序不恢复。

历史验证： [0.03](VALIDATION-0.03.md)、[0.02](VALIDATION-0.02.md)、[0.01](VALIDATION-0.01.md)。

## Windows 独立包

导出进程返回 0。独立 `RAVAGE.exe -- --smoke-test` 实际图形运行返回 0、错误日志为空：旧扫切、整塔林、原课程均通过；新场景 `breach=true panels=384`，`secondary=1 ruins=1 active=0`，最终 `EXPORT SMOKE RESULT PASS`。

嵌入资源 EXE：168,948,016 bytes；SHA-256 `67cf37094b3022198ac40fcfbd76fdb8aeb0bb205ddf7dd5c25c086afcee4a3c`。ZIP 校验值在同名 `.zip.sha256` 文件中；打包工具逐条 CRC 校验并比对 ZIP 内 EXE 散列。原 0.03 中文构建保留。
