# 空间技能表现 · 0.02

移动现在经过可见的入口：展开 0.22 秒、角色沉入 0.48 秒、出口显形 0.20 秒，随后释放原有速度。总展示时间约 0.9 秒。引导距离、出口安全验证、冲击速度、建筑伤害继续使用原机制。

| 操作 | 结果 |
|---|---|
| 普通状态按住左键或右键，松开 | 选出口 → 脚下展开入口 → 角色穿门 → 出口弹射 |
| Shift，再按 Shift | 循环下落蓄速 → 同样的穿门过程 → 高速弹射 |
| 长按空格，松开；右键蓄力再松开 | 定位并旋转切面 → 几何裂隙与切割弧 → 双门缓慢分离并保留 |
| V + 鼠标 / 滚轮 | 移动已完成的切割门或移动残留门 |
| F | 向瞄准的已连接门实际冲入；世界碰撞继续生效 |
| Q + 左/右键 | 保留原 A/B 表面放置操作 |
| C | 收回全部门；中断尚未完成的穿门 |
| X / R | 取消动作 / 回起点，清理角色裁切和临时特效 |

普通移动保留最近一对可双向通行的独立门，再次施放替换该对门。A/B 布置与切割共用另一对门；切割不删除移动残留门。最多四个持久门，最多两个门内视野渲染目标，优先显示离摄像机近的一对。移动残留门随 R 保留，C 或场景重建收回。未完成的施法被取消后角色恢复，已形成的门保留；出口受阻仍拒绝通行。

视觉采用黑色空间面、冷白边缘、低分辨率断续裂线、几何碎片和双层切割弧。背面的门面不遮住第三人称角色，入口/出口标签始终朝向摄像机。施法手有四片小型绕行碎片；放门、穿门、蓄力、切割、落地和冲击共享同一组颜色与形状。短音效为程序合成占位素材。

角色动画增加曲线采样、切割预备与回收、跑动反向摆臂、轻微转向侧倾，以及有相位差的披风摆动。穿门时只驱动表现层位移和门平面裁切；入口动画结束后，在物理帧复核出口再提交位移。出口显形完成后恢复动量。按键不会在过程中重入施法。

安全与预算：出口若被新落块挡住则取消；重置/取消/收门不会留下延迟传送；瞬时效果硬上限 96 个，施法手额外四个固定碎片，无粒子物理、无新布料模拟。门数和视野数量均有上限。基本动画仍有继续精修空间；这轮不改碰撞体为人形。

复现：`powershell -ExecutionPolicy Bypass -File tools/portal.ps1 -Skills` 运行真实渲染与右键输入测试并保存过程截图；`-Check` 运行全部无窗口回归。结果与截图在 `docs/portal-skills/`。普通游玩仍双击 `开始传送门原型.cmd`。

本轮验证：基础门 48、魔法 44、混合玩法 34、塔林 33、峡谷 22、角色 25、新技能流程 26，共 232 项无窗口检查通过；新流程另有 26 项真实渲染检查通过。覆盖原始 132 塔的局部破坏、残留门双向穿越、出口动态受阻、进门/出门阶段重置、途中收门和两对门共存。实际新特效峰值 53 个，低于 96 上限；这不是全场长期性能压力测试。
