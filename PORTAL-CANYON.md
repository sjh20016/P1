# Portal · 垂直峡谷与旋转切割

双击 **开始传送门原型.cmd**。Godot 项目的默认主场景也已改为该原型。

当前场景来自 `资产模型/VerticalCanyon.blend`，保留原布局、轮廓、材质颜色、桥梁、平台和出生点。源 Blender 文件未改动。运行时加载 370 个原始物件，其中 132 个是塔体与远景塔；它们均接入局部破坏，桥梁与平台也能受损。

## 操作

| 操作 | 效果 |
|---|---|
| 长按空格 | 短暂浮空，引导切割位置 |
| 松空格 | 锁定中心；鼠标改为旋转切割面，镜头保持稳定 |
| 按住右键 / 松开 | 扩大切割半径 / 按当前朝向完成切割；蓄力时仍能旋转 |
| 切割完成 | 双门沿切面法线在约 1.8 秒内平滑分开，各移动 2.8 m 后停止；随后持续存在并可穿越 |
| 瞄准切割门，按住 V | 移动该门，鼠标调整位置，滚轮调节远近；松 V 固定 |
| Shift / 再按 Shift | 回路蓄速 / 发射；角色外观在头脚门之间反复下坠，循环随速度加快，实际碰撞体与镜头不来回传送 |
| 左键按住 / 松开 | 引导出口 / 突进，亦可接在蓄速之后 |
| Q + 左 / 右键 | 在实体表面设置 A / B；F 冲门，G 改放 B |
| C / X | 收回实体双门 / 取消当前技能 |
| T / R / F5 | 投掷刚体 / 回到出生点 / 重建场景与破坏状态 |
| 1–8 | 沿资产原始路线切换测试位置 |
| F3 / Esc | 参数 / 释放鼠标 |

切割门使用现有物理 A/B 槽位：再次切割会替换上一对，场上最多保留一对连接。分离与拖动期间暂停穿越，防止移动门扫过玩家时误传送；固定后，角色与小型投掷物均可通过。出口堵塞时会拒绝穿越，不把角色塞入模型。

## 资产与性能实现

- `tools/export_portal_canyon.py` 在 Blender 后台读取原场景，把源三角形划入 8 m 空间单元。保留源坐标和颜色，并转换到 Godot 的 Y-up 坐标；不导入展示相机、标记文字、碎片库或重复碰撞源。
- `project/assets/canyon/layout.json` 记录源文件 SHA-256、导出统计和路线标记；`vertical_canyon.bin` 保存原始网格及逐物件压缩的破坏数据，约 46.6 MB。游戏运行不依赖 Blender。
- 完整物件使用原始低面数网格；首次受击才解压其破坏单元，随后只细分伤口涉及的原始面。存活几何合并为单个网格和碰撞体，没有逐格节点。
- 附近、运动前方及传送门附近启用碰撞；远处保留视觉和损伤记录。碎块沿用 48 个刚体上限。损伤在本次场景中保留，F5 重建后恢复。
- 旋转切割使用有厚度圆盘与包围盒的相交判定。当前资产的伤口按 8 m 单元移除，**并非任意角度的精确网格布尔切割**；大型塔体暂不模拟整塔结构倒塌，切断后剩余部分仍可悬空。这版优先保留原资产和广泛局部破坏。

保留 81 塔生成测试场及旧八站实验场作为回归夹具；日常启动使用指定的垂直峡谷。

## 验证与复现

```powershell
powershell -ExecutionPolicy Bypass -File tools/portal.ps1 -Check
powershell -ExecutionPolicy Bypass -File tools/portal.ps1 -Tour
```

`-Check` 覆盖旧传送门、魔法技能、混合玩法、81 塔夹具和最终峡谷；`-Tour` 在最终峡谷运行带画面和正常音效的测试并保存截图。报告分别在 `docs/portal001`、`docs/portal-magic`、`docs/portal-hybrid`、`docs/portal-forest`、`docs/portal-canyon`。

最终峡谷测试覆盖出生点真实落地、鼠标旋转、原模型受损、持久双门、移动门、动量穿越、分离途中替换、清门、蓄速外观复位、132 个塔体逐一破坏、休眠后的损伤保留与碎块预算。渲染报告中的帧时间是**132 个塔体受损后的固定飞行路线**，不代表所有硬件或所有极端破坏场景。

2026-09-20 验证：五组逻辑回归共 **180 项通过**；最终峡谷带画面测试 **22 项通过**，无失败。Godot 4.7.1 / Compatibility，RTX 5060 Laptop GPU，1280×800 窗口，WASAPI 音效开启。破坏后路线记录 1797 帧，P95 **6.115 ms**，最大 **13.304 ms**；该次逐塔测试的最慢单物件重建 **12.08 ms**。这些数值来自 `docs/portal-canyon/rendered-results.json`，不包含整套测试中截图读取等操作的耗时。

重新生成资产时使用 Blender 5.2 后台运行 `tools/export_portal_canyon.py`。正常迭代技能不必重新导出；更新源 Blender 场景后才需要执行。
