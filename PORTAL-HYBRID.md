# 空间破坏 · 技能与物理门共存

本轮将角色整理成两层能力：**技能负责快速调整自己的位置与速度，实体门负责建立可重复利用的世界路线。** 两层可以连用；技能突进不再覆盖场上的 A/B。仍是原型迭代，不代表手感已通过真人验收。

双击 **开始传送门原型.cmd**。第一次建议按 **F7**：面前是 A，通过它可以观察另一处塔楼；按 **T** 投送碎块，或按 **Shift** 蓄速后 **F** 冲门。按 **F5** 重建建筑，再比较两种攻击。

## 当前操作

| 操作 | 结果 |
| --- | --- |
| 左键按住 → 松开 | 保留引导式快速突进；脚下入口和出口为短暂技能表现，不占用实体 A/B |
| Shift → Shift | 头脚回路蓄速后沿视线发射；也可接左键选点 |
| **按住 Q + 左键 / 右键** | 在静态表面放置实体 A / B，松 Q 回到技能操作，无须切换整个角色模式 |
| **F** | 冲向视线内、30 m 内实体门的正面；必须真的到达入口，途中墙体仍会挡住角色 |
| **蓄速后 F** | 将存储速度释放为冲门运动，再由真实 A/B 转换方向与动量 |
| **G** | 在瞄准表面改放 B，保留 A；失败时保留旧出口 |
| **T** | 投放小型真实刚体；瞄准附近实体门时补偿一部分重力弹道，仍需实际飞到门内 |
| **L → 等待 → G** | 进入碎块重力循环预设；新模式中 A 在地面、B 在高处，改 B 可释放高速弹丸 |
| 长按空格 → 松开 → 右键蓄力释放 | 原有悬浮选点和双门分离切割 |
| C / X | 收回实体连接和短暂门表现 / 取消当前技能选点、蓄速 |
| R / F5 | 玩家复位并保留有效实体门 / 重建地图、保留操作模式 |
| F3 / F4 / F7 | 实时参数 / 旧版纯双门操作 / 新混合投送试验 |

实体 A/B 依然**双向**。A 入口、B 出口只是新试验场和 G 快捷键采用的常用布置，不会禁止反向穿越。旧模式的 L 预设顺序保持原样。门的支撑面被破坏或移动时，该门关闭；放门仍要求孔径范围内有足够的完整静态平面。

## 更多动作玩法：优先组合，保持操作数量可控

| 动作 | 玩家怎样做 | 独立价值 | 当前状态 |
| --- | --- | --- | --- |
| **窥门突袭** | 布置 A/B，靠近 A 观察出口附近的结构和碎块，再决定普通冲入或蓄速冲入 | 观察直接服务于进攻路线；玩家可以临时改变主意 | 实时视野、视差、F 冲门已实现并自动验证 |
| **蓄速穿塔** | 先布置稳定路线，Shift 蓄速后按 F | 准备过的路线能重复使用，不必每次高速飞行中重新瞄准出口 | 已验证实际入门、方向转换、撞击伤害 |
| **碎块投送** | 自己留在 A 一侧，把小刚体投进去，看它从 B 出现并撞墙 | 角色以外的物体也能成为攻击载体 | 已验证真实刚体穿越后伤害目的地建筑；T 目前是测试投块 |
| **重力炮台** | 地面 A、高处 B，让碎块反复下落加速；再用 G 将 B 改到朝向另一座塔的墙上 | 布置产生持续收益，改变一个出口就能把循环变为攻击 | 已验证 100 m/s 上限、改出口与撞塔链；不是按键直接赋予碎块满速 |
| **空中折返** | 物理门弹出后，用技能突进改向，再对准实体门 F 冲入 | 技能负责救线和换角度，实体门负责重复组织攻击 | 基础动作已具备；多轮连续手感仍待真人测试 |
| **高空落锤** | 利用地面入口承接坠落，出口放在可用天花板表面，朝下释放自己或碎块 | 同一速度可以转成水平穿刺或垂直砸击 | 原坐标转换和天花板放门已具备；新关卡路线未专门制作 |
| **切后换线** | 先在塔上切出弱点，随后改变 B，将自己或弹丸送向这个位置 | 切割负责改变结构条件，动量攻击负责完成倒塌 | 能力可以连用；尚未验证成稳定连招，不承诺所有切口都能触发倒塌 |

设计上应保留“布门有收益，但不用布门也能爽快行动”。技能突进只有短暂表现，不能替代持续投送、提前看路或重力循环；实体门也不应要求玩家反复进行精确的两点瞄准。当前提供 Q 修饰键、G 单端改位和 F 入门辅助，后续优先观察是否仍需要位置吸附，而非增加更多快捷键。

以下是**候选设计，尚未实现**，需要先确认上述组合值得重复：

| 候选 | 动作构思 | 制作前需要解决的问题 |
| --- | --- | --- |
| **截流反击** | 短暂在身前展开入口，将飞来的小碎块送往已布置出口 | 只接收符合质量/尺寸预算的实体，给出明确命中窗口；不要自动吞整栋建筑 |
| **切块接力** | 切割后短时间内，把一块合格的小碎块引导到最近实体入口 | 需要碎块选择和可读的吸附过程；仍走刚体碰撞，不能把全部碎块同时拉入 |
| **裂口续门** | 撞击成功后，在新暴露的完整表面给一次快速落门提示，接下一轮攻击 | 先解决断裂表面重判与门存活规则；破损孔洞不能被当作稳定墙面 |
| **关门断流** | 在合格小物体穿越时关闭连接，产生一次有限范围挤压或截断 | 必须先有可靠的跨门状态与物体两侧显示；目前直接关门不能据此判定切断 |

不建议同时制作四项。优先级是：让 **蓄速穿塔、重力炮台、空中折返** 的操作和反馈稳定，然后决定是否需要截流反击。

## 本轮实时门内视野

已经实现实体 A/B 的单层实时窗口。摄像机位置按同一套传送坐标关系映射到出口侧，视锥随玩家在入口前的位置偏移，近裁剪面位于出口平面外侧。侧身看门会产生视差，目的地物体移动和建筑状态变化来自实际世界。

使用 Godot 的 [SubViewport 纹理](https://docs.godotengine.org/en/stable/tutorials/shaders/using_viewport_as_texture.html) 和 [Camera3D 的非对称视锥](https://docs.godotengine.org/en/stable/classes/class_camera3d.html#class-camera3d-method-set-frustum)。这两个 API 的组合方式、坐标映射与门面裁剪属于本项目实现，已对投影角点和实际渲染分别测试。

- 最多两个复用的 SubViewport，共享现有 World3D；没有复制世界或增加刚体模拟。
- 默认每门 **512 × 512、30 Hz、90 m** 观察范围。只更新正面、视锥内且连接有效的门；F3 可关闭视野或调整分辨率和频率。
- 技能闪门、切割双门和头脚回路继续使用黑色占位表现，不各自创建实时摄像机。
- 渲染层 20 保留给门面，门内摄像机排除该层，避免递归采样；物理层含义不变。
- 目前不递归显示“门里面的另一个门”，也不提供经门射线选中另一侧物体、声音跨门传播或远区流式加载。这些均未因门内可见而自动实现。

## 动画制作方案与当前接口

**物理系统决定何时真正穿越；动画决定玩家怎样看见和感受穿越。** 高速情况下不能等一段固定长度动画结束后才传送，也不能让动画根位移再移动一次角色。

| 动作族 | 建议动画 | 与物理配合 |
| --- | --- | --- |
| 快速突进 | 脚下短踏、前手指向出口，躯干迅速前倾；起手约 3–6 帧作为初始试值 | 保持低输入延迟；出门后混合到当前飞行姿态，不锁住整个动作播放时长 |
| 实体穿门 | 保留跑、跳、坠落或冲刺姿态；出口方向改变时躯干先转、四肢跟随 | 两侧显示同一动画时间的身体，穿越发生在统一物理时刻 |
| 头脚蓄速 | 双脚收拢或屈膝悬浮，手部收束空间，衣摆和门缘运动随蓄速增强 | 动画读 `boost_speed / max_portal_velocity`，摄像机不跟随反复翻转 |
| 悬浮选点与切割 | 左手维持标记，右手收拢蓄力，释放时双手分开，呼应双门分离 | 切割视觉先展开，现有 80 ms 伤害延迟作为同步点；充能大小由实际半径驱动 |
| 撞击与落地 | 高能量撞击用短压缩姿态，随后恢复飞行或落地；普通落地单独处理 | 复用现有高能量顿挫，不把所有普通碰撞变成长硬直 |

新增 `PortalPresentation.animation_cue(action, data)`，统一转发 `link_aim`、`link_place`、`dash_aim`、`boost_start`、`dive`、`cut_edit`、`cut_lock`、`cut_charge`、`cut_release`、`passage`、`exit_blocked`、`impact`、`land`、`cancel`、`reset`。最近事件记录限制为 24 条。

`passage` 包含旅客 ID、技能/物理类型、入口/出口坐标系、入点/出点、速度、输入姿态和映射姿态；物理穿越另带旋转映射。角色表现接口过滤其他刚体的穿越事件。未来可由 AnimationTree、手部目标、披风特效和音效订阅这些数据。**本轮只提供接口与占位表现，没有制作参考角色的骨骼模型或动画片段。**

真正的“半身在门这边、半身在那边”仍需要下一步专门制作：

1. 给旅客增加进入、重叠、完成、退出状态，保存正在穿越的门对，防止改门时两半身体指向不同出口。
2. 保持一个物理身体，额外显示一个无碰撞的骨骼/模型副本，两侧使用相反的裁剪平面，并共享动画时间、骨骼姿态及材质参数。
3. 当前物理传送在包围球前缘接近门面时接管，用于避免实体支撑墙挡住旅客。成品需要协调中心穿越时刻、孔径范围内的支撑墙碰撞豁免和身体裁剪；仅加一段“跨门动画”无法解决这点。
4. 单独处理贴近门面、第三人称 SpringArm 穿过门面、极斜观察和门在重叠期间被摧毁。主摄像机保持重力方向稳定，不跟着角色做非必要的翻滚。

建议先用灰盒人形验证以上四点，再制作完整服装和长披风。长披风跨门时需要同样裁剪，布料模拟是否连续则另定预算。

## 代码、参数与验证

| 新增/修改模块 | 职责 |
| --- | --- |
| `portal_link_ability.gd` | 实体门输入、放置预览、冲门弹道和投块辅助 |
| `portal_manager.gd` | 分离技能门表现与物理 A/B；发出带坐标数据的穿越事件 |
| `portal_window_renderer.gd`、`portal_window.gdshader` | 两个复用窗口、坐标映射、非对称投影、圆形门面 |
| `portal_presentation.gd` | 角色动画/特效事件适配、撞击与落地事件 |
| `portal_magic.gd`、`portal_volume_cut.gd` | 输入协调、蓄速接冲门、能力阶段事件 |
| `portal_playground.gd`、`portal_debug_hud.gd`、`portal_profile.gd` | F7 投送场、L 的 A/B 顺序、说明及实时参数 |
| `test_portal_hybrid.gd`、`tools/portal.ps1` | 混合玩法、渲染输入、视差、资源回收与回归检查 |

默认 `link_dive_range=30 m`、`link_dive_speed=60 m/s`；高于该值的蓄速可保留至 `max_portal_velocity=100 m/s`。旧动量倍率保持 1.0、物理门半径 3.2 m、每旅客穿越冷却 0.18 秒，碎块/宏块仍受 48/3 预算限制。

验证命令：`tools/portal.ps1 -Check` 跑旧物理门、原技能和混合玩法；`-Tour` 跑原技能的带画面巡检；`-Tour -Hybrid` 跑新组合与实时门内视野。Godot 版本为本机 4.7.1。记录保存在 [docs/portal-hybrid](docs/portal-hybrid)，其中无界面通过命令边界测试，带画面通过 Godot 鼠标/键盘输入入口测试。

已验证的重点包括：Q 修饰键不会误触发突进或切割；先松 Q 再松鼠标也安全；技能不覆盖实体门；蓄速 F 真正穿门并撞塔；投块穿门并伤害建筑；重力循环限速、改出口与撞塔；普通墙体挡住 F；支撑面失效关闭窗口；反复重建仅保留两个窗口；目的地动态变化与侧向视差。

最终结果：混合玩法命令/物理 **34/34**，带画面输入/渲染 **42/42**；旧门 **48/48**，原技能命令/物理 **43/43**，原技能带画面 **43/43**。无失败、脚本错误或泄漏报告。最后一轮混合巡检发生 9 次刚体穿越，重力循环达到 100 m/s 后改出口成功并造成建筑伤害。2,323 帧的 P95 为 **6.326 ms**、最大 **61.85 ms**；这是短时原型巡检数据，不能代表长期开放地图性能。首次窗口试跑曾出现约 309 ms 峰值，窗口建立、缓存和破坏峰值仍需进一步区分测量。

截图：[门内目的地](docs/portal-hybrid/01-live-window.png)、[侧向观察](docs/portal-hybrid/02-parallax.png)、[碎块投送](docs/portal-hybrid/03-object-route.png)、[实体冲门撞击](docs/portal-hybrid/04-physical-impact.png)、[循环炮击释放](docs/portal-hybrid/05-loop-release.png)。

## 仍需真人试玩判断

- 预览和门缘目前仍简陋，白灰环境里的门内远景可能缺乏辨识度，需要空间裂纹、前后景对比和少量运动参考物来增强深度感。
- 一对实体门、两个短暂技能门、两个切割门可能同屏出现；只有实体门有实时视野，但视觉层级仍待调整。
- F 是一次冲刺，会被中途障碍挡住；极端侧向、近地或门边缘路线可能没有穿进孔径。当前没有磁吸穿墙式纠错。
- 放门在表面边缘可能失败；G 失败会保留旧门。若玩家明显停下来找准位置，下一步优先做有限范围吸附和出口有效性提示。
- 30 Hz 窗口有更新间隔；首次创建窗口和建筑破坏可能出现明显帧时间峰值。没有证明大地图、多宏块长期运行的性能稳定。
- 尚未完成真人对连续折返、眩晕、蓄速收益、布门时间的评估。建议用同一座塔分别试“直接技能撞击”“蓄速实体冲门”“循环碎块炮击”，确认三种路线各自有吸引力后再选候选动作。
